import React from 'react';
import {View, Text, Image, ScrollView, TextInput} from 'react-native';

const Deni = () => {
    <Image source ={require('./map-placeholder.png')} />;
    const icon = this.props.active
      ? require('./map-placeholder.png')
      : require('./map-placeholder.png');
    <Image source={icon} />;
}
